// ignore_for_file: file_names

import 'package:flutter/material.dart';

class DigitalMapping extends StatelessWidget {
  const DigitalMapping({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Digital Mapping'),
    );
  }
}