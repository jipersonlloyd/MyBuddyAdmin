// ignore_for_file: file_names

import 'package:flutter/material.dart';

class CustomerMapping extends StatelessWidget {
  const CustomerMapping({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Customer Mapping'),
    );
  }
}