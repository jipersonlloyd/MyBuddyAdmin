// ignore_for_file: file_names

import 'package:flutter/material.dart';

class TerritoryGeofencing extends StatelessWidget {
  const TerritoryGeofencing({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Territory Geofencing'),
    );
  }
}