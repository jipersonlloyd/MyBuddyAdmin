// ignore_for_file: file_names

import 'package:flutter/material.dart';

class Jobber extends StatelessWidget {
  const Jobber({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Jobber'),
    );
  }
}