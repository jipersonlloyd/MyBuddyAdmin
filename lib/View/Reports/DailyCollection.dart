// ignore_for_file: file_names

import 'package:flutter/material.dart';

class DailyCollection extends StatelessWidget {
  const DailyCollection({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Daily Collection'),
    );
  }
}