// ignore_for_file: file_names

import 'package:flutter/material.dart';

class TransactionLedger extends StatelessWidget {
  const TransactionLedger({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Transaction Ledger'),
    );
  }
}