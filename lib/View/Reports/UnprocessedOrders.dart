// ignore_for_file: file_names

import 'package:flutter/material.dart';

class UnprocessedOrders extends StatelessWidget {
  const UnprocessedOrders({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Unprocessed Orders'),
    );
  }
}